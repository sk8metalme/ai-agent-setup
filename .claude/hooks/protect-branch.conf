# protect-branch.conf
# 保護ブランチガード設定ファイル

# 保護対象のブランチ（パイプ区切り、正規表現）
PROTECTED_BRANCHES="main|master|develop"

# 禁止する git 操作（パイプ区切り、正規表現）
DANGEROUS_OPS="git commit|git push|git merge"

# ブロック時のメッセージ
BLOCK_MESSAGE="保護ブランチへの直接操作は禁止です。新しいブランチを作成してください。"
